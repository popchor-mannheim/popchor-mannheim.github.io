header:
  - titles:
      # @start locale config
      de      : &de       Über uns
      # @end locale config
    url: >-
      /#ber-den--chor
  - titles:
      # @start locale config
      de      : &de       Veranstaltungen
      # @end locale config
    url: >-
      /#kommende-events
  - titles:
      # @start locale config
      de      : &de       Mitsingen
      # @end locale config
    url: >-
      /#mitsingen
  - titles:
      # @start locale config
      de      : &de       Eindrücke
      # @end locale config
    url: >-
      /#eindrcke--presse
  - titles:
      # @start locale config
      de      : &de       Kontakt
      # @end locale config
    url: >-
      /#kontakt
